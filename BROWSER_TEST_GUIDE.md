# 🌐 浏览器前端测试指南

## 立即开始 (2 分钟)

### 第一步：打开浏览器

复制粘贴这个 URL 到浏览器地址栏：

```
http://localhost:8001/pages/review-session.html?user_id=test_user
```

### 第二步：按照下面的清单测试

---

## ✅ 完整功能测试清单

### 1️⃣ 页面加载 (应该看到)

页面打开后，你会看到：

```
┌─────────────────────────────────┐
│   MixRead - Vocabulary Review    │
│                                  │
│   Choose Review Type             │
│                                  │
│   [ Mixed ]  [ New ]  [ Review ] │
└─────────────────────────────────┘
```

**检查清单：**
- [ ] 页面标题显示 "MixRead - Vocabulary Review"
- [ ] 看到三个蓝色按钮：Mixed / New Words / Review Due
- [ ] 按 F12 打开开发者工具 → Console 标签签
- [ ] Console 中没有红色的 ❌ 错误信息
- [ ] Console 中应该看到绿色的日志信息

**如果看到错误：**
- ❌ `Uncaught TypeError: ReviewManager is not defined`
  - 解决：刷新页面（Ctrl+R 或 Cmd+R）
  - 或清除缓存（Ctrl+Shift+Delete）

- ❌ `404 - /pages/review-session.html not found`
  - 解决：确保前端服务运行在 http://localhost:8001
  - 重启前端：`python -m http.server 8001 --bind localhost`

---

### 2️⃣ 启动会话

**操作：** 点击 **"Mixed (New + Due)"** 按钮

**应该看到：**

```
┌─────────────────────────────────────┐
│  Progress: 1 / 5                    │
│  ████░░░░░░░░░░░░░░░░░░░░░░░░  20% │
├─────────────────────────────────────┤
│                                      │
│       ╭───────────────────╮          │
│       │                   │          │
│       │    serendipity    │          │
│       │                   │          │
│       ╰───────────────────╯          │
│                                      │
│    [ Show Answer ]                   │
├─────────────────────────────────────┤
│                                      │
│ Correct: 0  Streak: 0  Accuracy: 0% │
│ Time: 00:05                          │
│                                      │
└─────────────────────────────────────┘
```

**检查清单：**
- [ ] 看到进度条显示 "1 / 5"
- [ ] 看到一个单词（例如：serendipity, eloquent, ubiquitous）
- [ ] 看到 "Show Answer" 按钮
- [ ] 右侧显示统计信息（Correct, Streak, Accuracy, Time）
- [ ] 计时器在运行（Time 不断增加）

**如果卡片不显示：**
- 检查 Console 是否有错误信息
- 确保数据库中有测试数据
- 参考故障排除部分

---

### 3️⃣ 卡片翻转

**操作 1：按键盘上的 Space 键**

或

**操作 2：点击 "Show Answer" 按钮**

**应该看到：**

```
┌─────────────────────────────────────┐
│  Progress: 1 / 5                    │
│  ████░░░░░░░░░░░░░░░░░░░░░░░░  20% │
├─────────────────────────────────────┤
│                                      │
│       ╭───────────────────╮          │
│       │   Definition:    │          │
│       │ The occurrence   │          │
│       │ of events by     │          │
│       │ chance in a      │          │
│       │ happy or         │          │
│       │ beneficial way   │          │
│       │                  │          │
│       │ Example:         │          │
│       │ "It was pure     │          │
│       │ serendipity"     │          │
│       │                  │          │
│       │ CEFR: C1         │          │
│       ╰───────────────────╯          │
│                                      │
│ [ Again ] [ Hard ] [ Good ] [ Easy ] │
├─────────────────────────────────────┤
│ Correct: 0  Streak: 0  Accuracy: 0% │
│ Time: 00:08                          │
│                                      │
└─────────────────────────────────────┘
```

**检查清单：**
- [ ] 卡片翻转，显示定义
- [ ] 看到例句（Example）
- [ ] 看到 CEFR 等级（A1-C2）
- [ ] 看到 4 个评分按钮：Again / Hard / Good / Easy
- [ ] 按钮颜色分别为：红 / 橙 / 蓝 / 绿

**快捷键提示：**
- `Space` - 显示/隐藏答案
- `1` - 选择 "Again"
- `2` - 选择 "Hard"
- `3` - 选择 "Good"
- `4` - 选择 "Easy"

---

### 4️⃣ 提交答案

**操作 1：点击 "Easy" 按钮**

或

**操作 2：按 "4" 键**

**应该看到：**

```
┌─────────────────────────────────────┐
│  Progress: 2 / 5                    │
│  ████████░░░░░░░░░░░░░░░░░░░░░  40% │
├─────────────────────────────────────┤
│                                      │
│       ╭───────────────────╮          │
│       │    eloquent      │          │
│       │                   │          │
│       ╰───────────────────╯          │
│                                      │
│    [ Show Answer ]                   │
├─────────────────────────────────────┤
│                                      │
│ Correct: 1 🎉 Streak: 1  Accuracy: 100% │
│ Time: 00:12                          │
│                                      │
└─────────────────────────────────────┘
```

**检查清单：**
- [ ] 进度更新为 "2 / 5"
- [ ] 进度条向前推进
- [ ] 看到下一张卡片（新单词）
- [ ] Correct 计数增加到 1 ✅
- [ ] Streak 计数为 1
- [ ] Accuracy 显示 100%
- [ ] 计时器继续运行

---

### 5️⃣ 测试所有快捷键

重复操作，但使用不同的快捷键：

#### 测试 "Again" (1 键)
```
Current: 2 / 5, Correct: 1
→ 按 "1" 键 → Progress: 3 / 5, Correct: 1
```
Expected: 没有增加 Correct（因为又做错了）

#### 测试 "Hard" (2 键)
```
Current: 3 / 5, Correct: 1
→ 按 "2" 键 → Progress: 4 / 5, Correct: 1
```
Expected: 没有增加 Correct

#### 测试 "Good" (3 键)
```
Current: 4 / 5, Correct: 1
→ 按 "3" 键 → Progress: 5 / 5, Correct: 2
```
Expected: Correct 增加为 2 ✅

#### 测试 "Easy" (4 键)
```
Current: 5 / 5, Correct: 2
→ 按 "4" 键 → 显示完成屏幕
```
Expected: 看到会话完成屏幕

---

### 6️⃣ 会话完成屏幕

完成所有卡片后，会看到：

```
┌─────────────────────────────────────┐
│   Session Complete! 🎓               │
│                                      │
│   ✅ Cards Reviewed: 5              │
│   ✅ Correct: 3                     │
│   ✅ Accuracy: 60%                  │
│   ✅ Max Streak: 2                  │
│   ✅ Duration: 1:23                 │
│                                      │
│  [ Back to Library ] [ Start Another ]│
│                                      │
└─────────────────────────────────────┘
```

**检查清单：**
- [ ] 显示会话完成提示
- [ ] 显示所有统计数据
- [ ] 显示 "Back to Library" 按钮
- [ ] 显示 "Start Another" 按钮
- [ ] 可以点击按钮返回或开始新会话

---

## 🐛 浏览器控制台日志

按 **F12** 打开开发者工具，选择 **Console** 标签

### 应该看到的日志

```
[Review] Starting mixed session for user test_user
[Review] Session started: fe59354e-xxxx-xxxx-xxxx-xxxxxxxxxxxx, 5 cards
[Review] Current card: 1/5 - ubiquitous
[Review] Card shown - waiting for answer
[Review] Submitting answer: quality=4, time=2345ms
[Review] Answer received: { result: {...}, next_card: {...} }
[Review] Answer recorded - Correct: 1, Streak: 1
[Review] Current card: 2/5 - eloquent
...
[Review] Session ended: { cards_reviewed: 5, correct: 3, accuracy: 60, max_streak: 2, duration: "1:23" }
```

### 不应该看到的错误

- ❌ `Uncaught TypeError`
- ❌ `ReferenceError: ReviewManager is not defined`
- ❌ `Failed to fetch`
- ❌ `CORS error`
- ❌ `404 Not Found`

**如果看到这些错误，参考下面的故障排除部分。**

---

## 🔧 故障排除

### 问题 1：页面显示空白（完全没有内容）

**可能原因：**
- 前端服务未启动
- 访问错误的 URL
- JavaScript 文件未加载

**解决步骤：**

1. 检查 URL 是否正确：
   ```
   http://localhost:8001/pages/review-session.html?user_id=test_user
   ```

2. 打开浏览器开发者工具（F12）→ **Network** 标签

3. 刷新页面（Ctrl+R）

4. 检查是否有 **红色** 的请求（状态为 4xx 或 5xx）
   - 如果有，点击它查看详细信息
   - 检查是否是 404 Not Found

5. 如果显示 404：
   ```bash
   # 在新终端启动前端服务
   cd /Users/yinshucheng/code/creo/MixRead/frontend
   python -m http.server 8001 --bind localhost
   ```

6. 返回浏览器刷新页面

---

### 问题 2：页面加载了但没有按钮或卡片

**可能原因：**
- JavaScript 加载失败
- ReviewManager 类未初始化

**解决步骤：**

1. 打开浏览器控制台（F12 → Console）

2. 检查是否有红色错误信息

3. 尝试清除浏览器缓存：
   - **Chrome**: Ctrl+Shift+Delete → 勾选"Cookies and other site data" → 清除
   - **Firefox**: Ctrl+Shift+Delete → 勾选"Cookies"和"Cache" → 清除
   - **Safari**: Develop → Empty Web Storage

4. 关闭所有标签页，重新打开 URL

---

### 问题 3：卡片不显示单词

**可能原因：**
- 数据库中没有测试数据
- 后端未正确连接

**解决步骤：**

1. 打开控制台（F12 → Console）

2. 查看是否有 `Failed to fetch` 错误

3. 检查后端是否运行：
   ```bash
   curl http://localhost:8000/health
   ```

   应该看到：
   ```json
   {"status": "ok", "version": "0.2.0", ...}
   ```

4. 如果后端未运行，启动它：
   ```bash
   cd /Users/yinshucheng/code/creo/MixRead/backend
   python main.py
   ```

5. 添加测试数据：
   ```bash
   cd /Users/yinshucheng/code/creo/MixRead/backend
   python -c "
   from infrastructure.database import init_db, SessionLocal
   from infrastructure.models import VocabularyEntryModel
   from datetime import datetime

   init_db()
   db = SessionLocal()

   test_words = ['serendipity', 'ephemeral', 'quintessential', 'ubiquitous', 'eloquent']
   for word in test_words:
       db.add(VocabularyEntryModel(user_id='test_user', word=word))
   db.commit()
   print('✅ Test data added')
   "
   ```

6. 返回浏览器，刷新页面，再试一次

---

### 问题 4：按钮无响应（点击没有反应）

**可能原因：**
- JavaScript 事件监听器未正确绑定
- 类方法未正确定义

**解决步骤：**

1. 打开控制台（F12 → Console）

2. 手动测试 ReviewManager（在 Console 中输入）：
   ```javascript
   console.log('ReviewManager' in window)  // 应该显示 true
   console.log(typeof ReviewManager)        // 应该显示 'function'
   ```

3. 尝试清除缓存并刷新：
   - Ctrl+Shift+R（硬刷新，不使用缓存）

4. 如果仍然不工作，检查浏览器兼容性：
   - 使用最新版本的 Chrome、Firefox 或 Safari
   - 更新浏览器到最新版本

---

### 问题 5：CORS 错误

**错误信息示例：**
```
Access to XMLHttpRequest at 'http://localhost:8000/users/test_user/review/session'
from origin 'http://localhost:8001' has been blocked by CORS policy
```

**解决步骤：**

1. 检查后端是否正确配置了 CORS
   - 打开 `backend/main.py`
   - 检查是否有 `CORSMiddleware` 配置

2. 重启后端：
   ```bash
   # 停止当前后端进程（Ctrl+C）
   cd /Users/yinshucheng/code/creo/MixRead/backend
   python main.py
   ```

3. 清除浏览器缓存并刷新

4. 在浏览器控制台（F12）检查是否还有 CORS 错误

---

## ✅ 完整测试清单

在开始测试前，检查以下项目是否已完成：

### 环境准备
- [ ] 后端运行在 http://localhost:8000
- [ ] 前端运行在 http://localhost:8001
- [ ] 浏览器已打开并访问了正确的 URL
- [ ] 数据库中有至少 5 个测试单词

### 功能测试
- [ ] 页面正常加载，看到选择按钮
- [ ] 点击 "Mixed" 后看到卡片
- [ ] 按 Space 键翻转卡片
- [ ] 看到答案和4个评分按钮
- [ ] 快捷键 1-4 能正确提交答案
- [ ] 进度条和统计实时更新
- [ ] 完成所有卡片后看到完成屏幕

### 控制台检查
- [ ] 没有红色错误信息
- [ ] 看到绿色的 [Review] 日志信息
- [ ] ReviewManager 已正确初始化

---

## 🎯 测试完成后

如果所有测试都通过了，恭喜你！系统已经完全就绪！

### 后续步骤：

1. **检查数据保存**
   - 完成一个会话
   - 重启浏览器访问相同 URL
   - 检查数据是否被保存（如果实现了持久化）

2. **性能测试**
   - 打开多个会话标签
   - 检查是否有卡顿
   - 查看 Console 是否有警告

3. **跨浏览器测试**
   - 在 Chrome、Firefox、Safari 中都测试一遍
   - 检查是否有浏览器特定的问题

4. **用户反馈**
   - 邀请真实用户进行 beta 测试
   - 收集使用体验反馈

---

## 📞 快速支持

遇到问题？按以下顺序排查：

1. **检查终端输出** - 查看后端是否有错误信息
2. **打开浏览器控制台** - F12 → Console
3. **查看网络请求** - F12 → Network
4. **尝试硬刷新** - Ctrl+Shift+R
5. **清除缓存** - Ctrl+Shift+Delete

---

**准备好了吗？现在就打开浏览器开始测试吧！** 🚀
